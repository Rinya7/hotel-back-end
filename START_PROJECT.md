# Запуск проекту з нуля

> 📘 **Для кого ця інструкція:** Для новачків, які вперше запускають проект. Якщо ви вже знайомі з проектом - можете перейти до розділу "Сценарії використання".

---

## 🚀 Швидкий старт (покрокова інструкція)

### Передумови

**Що потрібно встановити:**

1. **Node.js** (версія 18 або новіша)
   - Завантажити з [nodejs.org](https://nodejs.org/)
   - Під час встановлення обов'язково вибрати "Add to PATH"
   - Перевірити встановлення: `node --version` (має показати версію)

2. **npm** (встановлюється разом з Node.js)
   - Перевірити: `npm --version` (має показати версію)

3. **Docker Desktop** (якщо використовуєте Docker)
   - Завантажити з [docker.com](https://www.docker.com/products/docker-desktop)
   - Встановити та запустити Docker Desktop
   - Перевірити: `docker --version` (має показати версію)

**Або замість Docker:**
- **PostgreSQL** (версія 12 або новіша)
  - Завантажити з [postgresql.org](https://www.postgresql.org/download/)
  - Встановити та запустити як службу

---

### Крок 1: Встановлення залежностей 📦

**Що робить:** Встановлює всі необхідні бібліотеки та залежності проекту.

**Як виконати:**

1. Відкрити термінал (Command Prompt, PowerShell, або Git Bash) в папці проекту
2. Виконати команду:
   ```bash
   npm install
   ```

**Що очікувати:**
- Команда завантажить всі залежності (це може зайняти 1-3 хвилини)
- В кінці має з'явитися повідомлення про успішне встановлення
- Створиться папка `node_modules/` (не видаляйте її!)

**Якщо помилка:**
- Перевірте, що Node.js встановлений: `node --version`
- Перевірте інтернет-з'єднання
- Спробуйте знову: `npm install`

---

### Крок 2: Налаштування бази даних 🗄️

**Що це:** База даних зберігає всю інформацію (користувачі, кімнати, бронювання, тощо).

#### Варіант A: Docker (рекомендовано для новачків) 🐳

**Що таке Docker:** Docker дозволяє запустити PostgreSQL в окремому контейнері без встановлення PostgreSQL на комп'ютер.

**Як виконати:**

1. Переконатися, що Docker Desktop запущений
2. Відкрити термінал
3. Виконати команду:
   ```bash
   docker run --name hotel-db \
     -e POSTGRES_PASSWORD=mysecretpassword \
     -e POSTGRES_USER=postgres \
     -e POSTGRES_DB=hotel \
     -p 5432:5432 \
     -d postgres
   ```

**Що робить команда:**
- `--name hotel-db` - називає контейнер "hotel-db"
- `-e POSTGRES_PASSWORD=...` - встановлює пароль для БД
- `-e POSTGRES_USER=postgres` - створює користувача "postgres"
- `-e POSTGRES_DB=hotel` - створює базу даних "hotel"
- `-p 5432:5432` - відкриває порт 5432 для підключення
- `-d` - запускає в фоновому режимі
- `postgres` - використовує образ PostgreSQL

4. Перевірити, що контейнер запущений:
   ```bash
   docker ps
   ```
   Має показати контейнер `hotel-db` зі статусом "Up".

**Якщо помилка "port is already allocated":**
- Порт 5432 вже зайнятий (див. "Проблема 4" нижче)

**Якщо помилка "name is already in use":**
- Контейнер вже існує (див. "Проблема 8" нижче)

#### Варіант B: Локальний PostgreSQL 💻

**Якщо не хочете використовувати Docker:**

1. Встановити PostgreSQL з [postgresql.org](https://www.postgresql.org/download/)
2. Під час встановлення вказати пароль для користувача `postgres`
3. Створити базу даних `hotel`:
   - Відкрити pgAdmin або командний рядок
   - Виконати: `CREATE DATABASE hotel;`

**⚠️ УВАГА:** Потрібно буде вказати правильний пароль в `.env` файлі (наступний крок).

---

### Крок 3: Налаштування змінних середовища ⚙️

**Що це:** Файл `.env` містить конфігурацію проекту (паролі, налаштування БД, тощо).

**Як виконати:**

1. Відкрити папку проекту в редакторі (VS Code, Notepad++, тощо)
2. Створити новий файл з назвою `.env` (з крапкою на початку!)
3. Скопіювати та вставити наступний вміст:

```env
# База даних
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=mysecretpassword
DB_NAME=hotel

# JWT (секретний ключ для токенів авторизації)
# ⚠️ ЗМІНІТЬ ЦЕ НА СВІЙ СЕКРЕТНИЙ КЛЮЧ!
JWT_SECRET=your-super-secret-key-here-change-this-in-production

# Супер-адмін (перший користувач системи)
SUPERADMIN_USERNAME=superadmin
SUPERADMIN_PASSWORD=your-superadmin-password-change-this
```

4. **ВАЖЛИВО:** Змінити значення:
   - `DB_PASSWORD` - має співпадати з паролем з Docker команди (або з вашим локальним PostgreSQL)
   - `JWT_SECRET` - змінити на будь-який довгий випадковий рядок (наприклад, `my-super-secret-jwt-key-12345`)
   - `SUPERADMIN_PASSWORD` - змінити на свій пароль для superadmin

5. Зберегти файл

**Якщо використовуєте локальний PostgreSQL:**
- `DB_PASSWORD` має бути паролем, який ви вказали під час встановлення PostgreSQL

**Якщо використовуєте інший порт для Docker:**
- Змінити `DB_PORT` на ваш порт (наприклад, `5433`)

---

### Крок 4: Перший запуск (створення таблиць) 🏗️

**Що це:** Міграції - це скрипти, які створюють структуру бази даних (таблиці, зв'язки між ними).

**Як виконати:**

1. Відкрити термінал в папці проекту
2. Застосувати міграції (створити таблиці):
   ```bash
   npm run db:migrate
   ```

**Що очікувати:**
- Команда виконає всі міграції
- Створяться всі необхідні таблиці в БД
- В кінці має з'явитися повідомлення про успішне виконання

**Якщо помилка "password authentication failed":**
- Перевірте `.env` файл (пароль має співпадати)
- Перевірте, що PostgreSQL запущений

**Якщо помилка "relation does not exist":**
- Це нормально для першого запуску
- Просто продовжуйте

3. Створити супер-адміна (першого користувача):
   ```bash
   npm run seed:superadmin
   ```

**Що очікувати:**
- Створиться користувач з логіном `superadmin` (або тим, що вказано в `.env`)
- Пароль буде той, що вказано в `SUPERADMIN_PASSWORD` в `.env`

**Якщо помилка "User already exists":**
- Це нормально - користувач вже існує
- Пароль оновиться на той, що в `.env`

---

### Крок 5: Запуск сервера 🚀

**Як виконати:**

1. Відкрити термінал в папці проекту
2. Запустити сервер в режимі розробки:
   ```bash
   npm run dev
   ```

**Що робить:**
- Компілює TypeScript код
- Запускає сервер на порту 3000
- Автоматично перезапускає при зміні файлів

**Що очікувати:**
- В консолі має з'явитися повідомлення типу:
  ```
  Server is running on port 3000
  Database connected successfully
  ```
- Сервер працює і чекає запитів

**Якщо помилка "port 3000 is already in use":**
- Порт 3000 зайнятий (див. "Проблема 5" нижче)

**Якщо помилка "Cannot connect to database":**
- Перевірте, що PostgreSQL запущений
- Перевірте `.env` файл

**Альтернатива (звичайний запуск без автоперезавантаження):**
```bash
npm start
```

---

### Крок 6: Перевірка роботи ✅

**Як перевірити, що все працює:**

1. **Відкрити Swagger UI (інтерактивна документація):**
   - Відкрити браузер
   - Перейти на http://localhost:3000/docs
   - Має з'явитися сторінка з документацією API

2. **Спробувати залогінитись (через Swagger):**
   - На сторінці Swagger знайти `POST /auth/login`
   - Натиснути "Try it out"
   - Ввести:
     ```json
     {
       "username": "superadmin",
       "password": "your-superadmin-password"
     }
     ```
   - Натиснути "Execute"
   - Має повернутися токен (довгий рядок)

3. **Або через термінал (curl):**
   ```bash
   curl -X POST http://localhost:3000/auth/login \
     -H "Content-Type: application/json" \
     -d '{"username": "superadmin", "password": "your-superadmin-password"}'
   ```

**Якщо все працює:**
- ✅ Сервер запущений
- ✅ База даних підключена
- ✅ Можна працювати з API

**Якщо помилка 401 (Unauthorized):**
- Перевірте правильність пароля в `.env` та в запиті
- Переконайтеся, що `npm run seed:superadmin` виконано успішно

---

## 🎉 Готово!

Якщо всі кроки виконано успішно - проект запущений! Тепер можна:
- Використовувати API через Swagger UI
- Розробляти нові функції
- Тестувати endpoints

**Далі:** Перейдіть до розділу "Сценарії використання" для роботи з проектом.

---

## 📋 Команди розробки (довідник)

### База даних

#### `npm run db:migrate`
**Що робить:** Застосовує всі міграції (створює/оновлює таблиці в БД)

**Коли використовувати:**
- Перший запуск проекту
- Після отримання нових міграцій з Git
- Після зміни entities (після `db:gen:init`)

**Приклад виводу:**
```
Migration executed: 1754930036124-init_schema
Migration executed: 1754930036125-fix_room_unique
...
```

#### `npm run db:revert`
**Що робить:** Відкатує (видаляє) останню застосовану міграцію

**Коли використовувати:**
- Якщо остання міграція має помилки
- Якщо потрібно повернути БД до попереднього стану

**⚠️ УВАГА:** Може видалити дані! Використовуйте обережно.

#### `npm run db:gen:init`
**Що робить:** Генерує нову міграцію на основі змін в entities

**Коли використовувати:**
- Після зміни файлів в `src/entities/` (додали поле, змінили тип, тощо)
- Перед тим як застосувати міграцію (`db:migrate`)

**Послідовність:**
1. Змінити entity (наприклад, додати поле в `Admin.ts`)
2. Запустити `npm run db:gen:init` - створить файл міграції
3. Перевірити згенерований файл в `src/migrations/`
4. Запустити `npm run db:migrate` - застосує міграцію

**Приклад:**
```bash
# 1. Змінили Admin.ts (додали нове поле)
# 2. Генеруємо міграцію
npm run db:gen:init

# 3. Перевіряємо створений файл
# src/migrations/1234567890-AddNewField.ts

# 4. Застосовуємо
npm run db:migrate
```

#### `npm run seed:superadmin`
**Що робить:** Створює або оновлює супер-адміна в БД

**Коли використовувати:**
- Перший запуск проекту
- Якщо потрібно змінити пароль superadmin
- Якщо видалили superadmin з БД

**Як змінити пароль:**
1. Змінити `SUPERADMIN_PASSWORD` в `.env`
2. Запустити `npm run seed:superadmin`
3. Новий пароль буде застосований

### OpenAPI (документація API)

#### `npm run openapi:bundle`
**Що робить:** Збирає OpenAPI документацію з окремих файлів в один

**Коли використовувати:**
- Після зміни файлів в `openapi/paths/`
- Перед деплоєм (щоб документація була актуальною)

**Що робить:**
- Читає `openapi/openapi.base.yaml`
- Читає всі файли з `openapi/paths/`
- Збирає все в `openapi/openapi.yaml`

#### `npm run openapi:lint`
**Що робить:** Перевіряє валідність OpenAPI схеми

**Коли використовувати:**
- Після зміни OpenAPI файлів
- Перед комітом (щоб переконатися, що схема валідна)

**Якщо помилка:**
- Виправити помилки в OpenAPI файлах
- Запустити знову

---

## 🔄 Типові сценарії використання

### Сценарій 1: Новий проект (перший запуск) 🆕

**Коли:** Ви тільки що склонували проект або вперше запускаєте.

**Послідовність:**
1. ✅ `npm install` - встановити залежності
2. ✅ Налаштувати `.env` файл (скопіювати приклад вище)
3. ✅ Запустити PostgreSQL (Docker або локальний)
4. ✅ `npm run db:migrate` - створити таблиці
5. ✅ `npm run seed:superadmin` - створити першого користувача
6. ✅ `npm run dev` - запустити сервер

**Очікуваний результат:**
- Сервер працює на http://localhost:3000
- Swagger UI доступний на http://localhost:3000/docs
- Можна залогінитись як superadmin

---

### Сценарій 2: Після зміни entities (додали нове поле) 🔧

**Коли:** Ви змінили структуру таблиць (додали поле, змінили тип, тощо).

**Послідовність:**
1. ✅ Змінити entity файл (наприклад, `src/entities/Admin.ts`)
2. ✅ `npm run db:gen:init` - згенерувати міграцію
3. ✅ Перевірити згенерований файл в `src/migrations/`
4. ✅ `npm run db:migrate` - застосувати міграцію
5. ✅ `npm run dev` - запустити сервер

**Приклад:**
```typescript
// 1. Додали поле в Admin.ts
@Column()
newField!: string;

// 2. Генеруємо міграцію
npm run db:gen:init

// 3. Застосовуємо
npm run db:migrate
```

**⚠️ УВАГА:** Не змінюйте вручну файли міграцій! Якщо потрібно - згенеруйте нову.

---

### Сценарій 3: Після видалення БД або створення нової 💾

**Коли:** Видалили базу даних або створили нову порожню БД.

**Послідовність:**
1. ✅ Запустити PostgreSQL (якщо не запущений)
2. ✅ Створити базу даних `hotel` (якщо не існує)
3. ✅ Перевірити `.env` файл (правильні дані для підключення)
4. ✅ `npm run db:migrate` - створити всі таблиці
5. ✅ `npm run seed:superadmin` - створити superadmin
6. ✅ `npm run dev` - запустити сервер

**Якщо використовуєте Docker:**
```bash
# Видалити старий контейнер (якщо є)
docker rm -f hotel-db

# Створити новий
docker run --name hotel-db \
  -e POSTGRES_PASSWORD=mysecretpassword \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_DB=hotel \
  -p 5432:5432 \
  -d postgres

# Далі кроки 4-6
```

---

### Сценарій 4: Повний ресет (видалити все і почати заново) 🔄

**Коли:** Потрібно повністю очистити БД і почати з нуля.

**⚠️ УВАГА:** Це видалить всі дані! Використовуйте тільки в розробці.

**Послідовність:**
1. ✅ Зупинити сервер (Ctrl+C)
2. ✅ `npm run db:revert` - відкатити всі міграції (видалити таблиці)
3. ✅ `npm run db:migrate` - застосувати міграції знову (створити таблиці)
4. ✅ `npm run seed:superadmin` - створити superadmin
5. ✅ `npm run dev` - запустити сервер

**Альтернатива (якщо використовуєте Docker):**
```bash
# Видалити контейнер з БД
docker rm -f hotel-db

# Створити новий
docker run --name hotel-db \
  -e POSTGRES_PASSWORD=mysecretpassword \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_DB=hotel \
  -p 5432:5432 \
  -d postgres

# Далі кроки 3-5 зверху
```

---

### Сценарій 5: Після отримання оновлень з Git 📥

**Коли:** Ви отримали нові зміни з Git (нові міграції, новий код).

**Послідовність:**
1. ✅ `git pull` - отримати зміни
2. ✅ `npm install` - оновити залежності (якщо змінився package.json)
3. ✅ `npm run db:migrate` - застосувати нові міграції (якщо є)
4. ✅ `npm run dev` - запустити сервер

**Якщо помилка "Migration already executed":**
- Це нормально - міграція вже застосована
- Просто продовжуйте

**Якщо конфлікт міграцій:**
- Перевірте логи
- Можливо потрібно відкатити та застосувати знову

---

## 🐛 Часті проблеми та рішення

### Проблема 1: "password authentication failed" ❌

**Помилка:**
```
Error: password authentication failed for user "postgres"
```

**Причини:**
- Неправильний пароль в `.env`
- Пароль в `.env` не співпадає з паролем в Docker
- PostgreSQL не запущений

**Рішення:**

1. **Перевірити `.env` файл:**
   ```env
   DB_PASSWORD=mysecretpassword
   ```
   Пароль має співпадати з тим, що ви вказали в Docker команді.

2. **Перевірити, що PostgreSQL запущений:**
   ```bash
   docker ps
   ```
   Має показати контейнер `hotel-db` зі статусом "Up".

3. **Якщо використовуєте локальний PostgreSQL:**
   - Перевірте правильність пароля в `.env`
   - Перевірте, що PostgreSQL запущений як служба

4. **Якщо не пам'ятаєте пароль:**
   ```bash
   # Зупинити контейнер
   docker stop hotel-db
   
   # Видалити контейнер
   docker rm hotel-db
   
   # Створити новий з новим паролем
   docker run --name hotel-db \
     -e POSTGRES_PASSWORD=новий-пароль \
     -e POSTGRES_USER=postgres \
     -e POSTGRES_DB=hotel \
     -p 5432:5432 \
     -d postgres
   
   # Оновити .env файл
   DB_PASSWORD=новий-пароль
   ```

---

### Проблема 2: "relation does not exist" ❌

**Помилка:**
```
Error: relation "admin" does not exist
```

**Причина:** Таблиці в БД не створені (міграції не застосовані).

**Рішення:**

```bash
# Застосувати міграції
npm run db:migrate
```

**Якщо не допомогло:**

```bash
# Відкатити всі міграції
npm run db:revert

# Застосувати знову
npm run db:migrate
```

**Якщо все ще помилка:**
- Перевірте, що підключення до БД працює
- Перевірте, що база даних `hotel` існує
- Перевірте логи сервера

---

### Проблема 3: "No migrations are pending" ❌

**Помилка:**
```
No migrations are pending
```

**Причина:** Всі міграції вже застосовані.

**Коли це нормально:**
- Після першого запуску (всі міграції застосовані)
- Після `npm run db:migrate`

**Коли це проблема:**
- Якщо ви очікували, що міграція застосується
- Якщо змінили entities, але не згенерували міграцію

**Рішення:**

Якщо змінили entities:
```bash
# 1. Згенерувати міграцію
npm run db:gen:init

# 2. Застосувати
npm run db:migrate
```

---

### Проблема 4: Порт 5432 зайнятий ❌

**Помилка:**
```
Error: bind: address already in use :::5432
```

**Причина:** Інший PostgreSQL вже використовує порт 5432.

**Рішення:**

**Варіант A: Зупинити інший PostgreSQL**

```bash
# Знайти контейнер, який використовує порт
docker ps

# Зупинити його
docker stop <container-name>
```

**Варіант B: Використати інший порт**

1. Змінити Docker команду:
   ```bash
   docker run --name hotel-db \
     -e POSTGRES_PASSWORD=mysecretpassword \
     -e POSTGRES_USER=postgres \
     -e POSTGRES_DB=hotel \
     -p 5433:5432 \  # Змінили порт на 5433
     -d postgres
   ```

2. Змінити `.env`:
   ```env
   DB_PORT=5433
   ```

---

### Проблема 5: Порт 3000 зайнятий ❌

**Помилка:**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**Причина:** Інший сервер вже використовує порт 3000.

**Рішення:**

**Варіант A: Зупинити інший сервер**

Знайти процес:
```bash
# Windows
netstat -ano | findstr :3000

# Linux/Mac
lsof -i :3000
```

Зупинити процес або закрити інший термінал з сервером.

**Варіант B: Використати інший порт**

Змінити `.env`:
```env
PORT=3001
```

Потім сервер буде доступний на http://localhost:3001

---

### Проблема 6: "Cannot find module" ❌

**Помилка:**
```
Error: Cannot find module 'express'
```

**Причина:** Залежності не встановлені або `node_modules/` пошкоджений.

**Рішення:**

```bash
# Видалити node_modules та lock файл
rm -rf node_modules package-lock.json

# Встановити заново
npm install
```

---

### Проблема 7: "JWT_SECRET is not set" ❌

**Помилка:**
```
Error: JWT_SECRET is not set. Please configure it in .env
```

**Причина:** В `.env` файлі відсутній `JWT_SECRET`.

**Рішення:**

1. Відкрити `.env` файл
2. Додати рядок:
   ```env
   JWT_SECRET=your-super-secret-key-change-this
   ```
3. Перезапустити сервер

---

### Проблема 8: Docker контейнер не запускається ❌

**Помилка:**
```
Error response from daemon: Conflict. The container name "hotel-db" is already in use.
```

**Причина:** Контейнер з таким ім'ям вже існує.

**Рішення:**

```bash
# Видалити старий контейнер
docker rm -f hotel-db

# Створити новий
docker run --name hotel-db \
  -e POSTGRES_PASSWORD=mysecretpassword \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_DB=hotel \
  -p 5432:5432 \
  -d postgres
```

---

### Проблема 9: Сервер не запускається після змін ❌

**Симптоми:**
- Сервер не запускається
- Помилки компіляції TypeScript
- Помилки імпортів

**Рішення:**

1. **Перевірити помилки в консолі:**
   - Прочитати повідомлення про помилку
   - Виправити помилки в коді

2. **Перевірити імпорти:**
   ```typescript
   // Правильно
   import { Something } from "./path";
   
   // Неправильно
   import { Something } from "./path.ts"; // Не потрібен .ts
   ```

3. **Очистити кеш:**
   ```bash
   # Видалити node_modules
   rm -rf node_modules
   
   # Перевстановити
   npm install
   ```

---

### Проблема 10: Міграції не застосовуються ❌

**Симптоми:**
- `npm run db:migrate` не створює таблиці
- Помилки при застосуванні міграцій

**Рішення:**

1. **Перевірити підключення до БД:**
   ```bash
   # Спробувати підключитися вручну
   psql -h localhost -U postgres -d hotel
   ```

2. **Перевірити міграції:**
   ```bash
   # Подивитися які міграції є
   ls src/migrations/
   ```

3. **Перевірити логи:**
   - Подивитися детальні логи помилки
   - Можливо міграція має синтаксичну помилку

4. **Повний ресет (якщо нічого не допомагає):**
   ```bash
   npm run db:revert
   npm run db:migrate
   ```

---

## 🔧 Корисні інструменти для розробки

### DBeaver (візуальний перегляд БД) 🗄️

**Що це:** Безкоштовна програма для роботи з базами даних. Дозволяє візуально переглядати таблиці, дані, виконувати SQL запити.

**Як встановити:**
1. Завантажити з [dbeaver.io](https://dbeaver.io/)
2. Встановити (просто "Next" → "Next" → "Install")

**Як підключитися:**

1. Відкрити DBeaver
2. Натиснути "New Database Connection" (іконка зі штекером)
3. Вибрати "PostgreSQL"
4. Натиснути "Next"
5. Заповнити налаштування:
   - **Host:** `localhost`
   - **Port:** `5432`
   - **Database:** `hotel`
   - **Username:** `postgres`
   - **Password:** `mysecretpassword` (або ваш пароль)
6. Натиснути "Test Connection"
7. Якщо "Connected" - натиснути "Finish"

**Що можна робити:**
- Переглядати таблиці та дані
- Виконувати SQL запити
- Експортувати/імпортувати дані
- Переглядати структуру таблиць

---

### VS Code розширення 💻

**Рекомендовані розширення:**

1. **TypeScript Importer**
   - Автоматично додає імпорти
   - Швидкий доступ до типів

2. **REST Client**
   - Тестування API прямо в VS Code
   - Створюйте файли `.http` з запитами

3. **PostgreSQL**
   - Підключення до БД прямо в VS Code
   - Виконання SQL запитів

**Як встановити:**
1. Відкрити VS Code
2. Натиснути іконку розширень (ліворуч)
3. Знайти розширення по назві
4. Натиснути "Install"

**Приклад використання REST Client:**

Створити файл `test.http`:
```http
### Логін
POST http://localhost:3000/auth/login
Content-Type: application/json

{
  "username": "superadmin",
  "password": "your-password"
}
```

Натиснути "Send Request" над запитом.

---

### Postman (тестування API) 📮

**Що це:** Програма для тестування API endpoints.

**Як встановити:**
1. Завантажити з [postman.com](https://www.postman.com/)
2. Встановити

**Як імпортувати API схему:**

1. Відкрити Postman
2. Натиснути "Import" (ліворуч вгорі)
3. Вибрати вкладку "Link"
4. Ввести URL: `http://localhost:3000/docs.json`
5. Натиснути "Continue" → "Import"

**Що отримаєте:**
- Всі API endpoints готові до тестування
- Автоматична документація
- Можливість зберігати запити

**Альтернатива:** Використовувати Swagger UI (http://localhost:3000/docs) - не потрібно встановлювати.

---

## 📖 Документація та корисні посилання

### Онлайн документація

- **Swagger UI:** http://localhost:3000/docs
  - Інтерактивна документація API
  - Можна тестувати endpoints прямо в браузері
  - Не потрібно встановлювати додаткові програми

- **API JSON схема:** http://localhost:3000/docs.json
  - OpenAPI схема в JSON форматі
  - Використовується для імпорту в Postman

- **OpenAPI схема (файл):** `openapi/openapi.yaml`
  - Вихідний файл схеми
  - Можна редагувати вручну

### Структура проекту

- **TASK.md** - завдання та прогрес проекту
- **README.md** - загальна інформація про проект
- **src/INFO.md** - опис структури проекту
- **src/*/INFO.md** - опис кожної папки

---

## 🚀 Запуск в Production (для сервера)

**Відмінності від розробки:**

1. **Змінні середовища:**
   - Встановити всі змінні на сервері
   - Не використовувати `.env` файл (використати системні змінні)

2. **Безпека:**
   - Змінити `JWT_SECRET` на дуже складний
   - Змінити `SUPERADMIN_PASSWORD` на безпечний
   - Налаштувати SSL для БД (якщо потрібно)

3. **Запуск:**
   ```bash
   # Застосувати міграції
   npm run db:migrate
   
   # Створити superadmin
   npm run seed:superadmin
   
   # Запустити сервер
   npm start
   ```

4. **Process Manager (рекомендовано):**
   - Використовувати PM2 або systemd
   - Автоматичний перезапуск при падінні
   - Логування

---

## 📞 Додаткова допомога

### Якщо нічого не допомагає

1. **Перевірити логи сервера:**
   - Подивитися детальні помилки в консолі
   - Шукати ключові слова помилки в Google

2. **Перевірити базові речі:**
   - ✅ Node.js встановлений
   - ✅ PostgreSQL запущений
   - ✅ `.env` файл створений правильно
   - ✅ Всі залежності встановлені (`npm install`)

3. **Повний ресет:**
   ```bash
   # Видалити node_modules
   rm -rf node_modules package-lock.json
   
   # Видалити Docker контейнер
   docker rm -f hotel-db
   
   # Почати з Кроку 1 інструкції
   ```

4. **Перевірити документацію:**
   - `README.md` - загальна інформація
   - `TASK.md` - поточний стан проекту
   - `src/*/INFO.md` - опис структури

### Корисні команди для діагностики

```bash
# Перевірити версію Node.js
node --version

# Перевірити версію npm
npm --version

# Перевірити Docker контейнери
docker ps

# Перевірити підключення до БД
docker exec -it hotel-db psql -U postgres -d hotel

# Перевірити логи сервера
# (в терміналі де запущений npm run dev)
```

---

## ✅ Чек-лист успішного запуску

Після виконання всіх кроків ви маєте:

- ✅ Сервер запущений на http://localhost:3000
- ✅ Swagger UI доступний на http://localhost:3000/docs
- ✅ Можна залогінитись як superadmin
- ✅ База даних містить всі таблиці
- ✅ В консолі немає помилок

**Якщо все вище виконано - проект успішно запущений! 🎉**

---

**Останнє оновлення:** 2025-01-XX
**Версія інструкції:** 2.0