# Controllers (Обробники запитів)

## Що це?
Контролери — це обробники HTTP запитів. Вони отримують запити від клієнта, викликають відповідні сервіси для обробки бізнес-логіки, і повертають відповіді.

## Структура
```
src/controllers/
├── auth.controller.ts           # Аутентифікація та користувачі
├── roomController.ts           # Управління кімнатами
├── roomAvailabilityController.ts # Доступність кімнат
├── roomPolicy.controller.ts    # Політики кімнат
├── stayController.ts           # Проживання
├── stayOps.controller.ts       # Операції з проживаннями
├── stayQuery.controller.ts     # Запити по проживаннях
├── guest.controller.ts         # Гостьовий доступ (токени та перегляд даних)
└── audit.controller.ts        # Аудит логів
```

## Основні контролери

### auth.controller.ts
- **loginAdmin()** — аутентифікація користувача з перевіркою блокировки
  - Перевірка блокировки адміна (`admin.isBlocked`)
  - Перевірка блокировки редактора (`admin.isBlocked` або `admin.createdBy?.isBlocked`)
  - Повертає специфічне повідомлення для заблокованих користувачів
- **createAdminBySuperadmin()** — створення адміна (тільки superadmin)
  - Підтримка Wi-Fi параметрів (`defaultWifiName`, `defaultWifiPassword`)
  - Підтримка політик заїзду/виїзду (`checkInHour`, `checkOutHour`)
- **createEditorAdmin()** — створення редактора (тільки admin)
- **getUsers()** — список користувачів (для superadmin повертає всіх адмінів з Wi-Fi параметрами)
- **getProfile()** — профіль користувача (для superadmin повертає список готелів з Wi-Fi параметрами)
- **blockAdmin()** / **unblockAdmin()** — блокування адміна (автоматично блокує всіх його редакторів)
- **blockEditor()** / **unblockEditor()** — блокування редактора
- **updateAdminHotelProfile()** — оновлення профілю готелю (підтримка Wi-Fi параметрів)
- **deleteAdminOrEditor()** — видалення користувача

### roomController.ts
- **getAllRooms()** — всі кімнати (superadmin)
- **getRooms()** — кімнати поточного готелю
- **createRoom()** — створення кімнати
- **updateRoom()** — редагування кімнати
- **deleteRoom()** — видалення кімнати
- **updateRoomStatus()** — зміна статусу кімнати

### stayController.ts
- **getStaysByRoom()** — історія проживань кімнати
- **createStay()** — створення бронювання/заселення
- **closeStay()** — закриття проживання

### guest.controller.ts
- **createGuestAccessLink()** — створення/отримання токену доступу для гостя (POST /guest/stays/:stayId/link)
- **getGuestAccessByToken()** — отримання даних проживання по токену (GET /guest/access/:token)

## Як працює?
1. **Отримання запиту** — Express передає запит в контролер
2. **Валідація** — перевірка даних через OpenAPI валідатор
3. **Авторизація** — перевірка прав через authMiddleware
4. **Бізнес-логіка** — виклик відповідного сервісу
5. **Відповідь** — повернення результату клієнту

## Приклад
```typescript
// roomController.ts
export const createRoom = async (req: Request, res: Response) => {
  try {
    // 1. Отримуємо дані з запиту (вже валідовані)
    const roomData = req.body;
    
    // 2. Викликаємо сервіс
    const room = await roomService.createRoom(roomData, req.user.adminId);
    
    // 3. Повертаємо результат
    res.status(201).json({
      message: 'Кімната створена',
      room
    });
  } catch (error) {
    // 4. Обробка помилок
    res.status(400).json({ message: error.message });
  }
};
```

## Права доступу
- **superadmin** — доступ до всіх контролерів
- **admin** — доступ до кімнат та користувачів свого готелю
- **editor** — тільки зміна статусів та робота з проживаннями
- **guest** — доступ тільки через токен доступу (без авторизації)

---

## Хід роботи (Guest функціональність)

### ✅ ВИКОНАНО:

**Етап 1-2 (Частина):**
- ✅ Створено entity `GuestAccessToken` (src/entities/GuestAccessToken.ts)
  - Поля: id, stay, token, createdAt, updatedAt, expiresAt, revokedAt, lastUsedAt
  - Зв'язок ManyToOne з Stay
- ✅ Додано зв'язок Stay ↔ GuestAccessToken (OneToMany/ManyToOne в Stay.ts)
- ✅ Винесено типи в src/types/guest.ts:
  - `GuestStayStatus` — тип статусу проживання
  - `GuestStayView` — DTO для відповіді гостю
  - `GenerateTokenResponse` — відповідь при генерації токену
  - `GuestAccessErrorResponse` — тип для помилок доступу
- ✅ Визначено логіку рівнів доступу за статусами Stay (booked, occupied, completed, cancelled)

**Етап 2 (Реалізація бекенда):**
- ✅ Створено контролер `guest.controller.ts`:
  - `createGuestAccessLink()` — генерація токену для Stay (з перевіркою прав admin/editor)
  - `getGuestAccessByToken()` — отримання даних проживання по токену (публічний ендпоінт)
  - `generateSecureToken()` — безпечна генерація токенів через crypto.randomBytes
  - `mapStayToGuestView()` — маппінг Stay+Room+Admin → GuestStayView
  - `formatHotelAddress()` — форматування адреси готелю
  - `formatPhoneNumber()` — форматування телефону
- ✅ Створено роути `guest.routes.ts`:
  - POST `/guest/stays/:stayId/link` (з authMiddleware, isEditorOrAdmin)
  - GET `/guest/access/:token` (публічний, без auth)
- ✅ Підключено роути в app.ts
- ✅ Виправлено імпорти (authMiddleware, AppDataSource)
- ✅ Реалізовано логіку показу Wi-Fi тільки для статусу `occupied`
- ✅ Додано валідацію токенів (перевірка expiresAt, revokedAt)
- ✅ Додано оновлення lastUsedAt при використанні токену

### 📋 ЗАЛИШИЛОСЬ:

**Етап 3 (Фронтенд PWA):**
- ⏳ Проєктування структури сторінок та роутів Vue
- ⏳ Реалізація екранів для різних статусів Stay
- ⏳ Інтеграція з бекенд API

**Етап 4 (Додаткові функції):**
- ⏳ POST `/guest/access/:token/requests/cleaning` — запит на прибирання
- ⏳ POST `/guest/access/:token/feedback` — залишити відгук
- ⏳ POST `/guest/access/:token/extend` — продовжити проживання
- ⏳ Додати коментарі/побажання гостя (в майбутньому)

**Тестування:**
- ⏳ Перевірити генерацію токенів
- ⏳ Перевірити доступ по токену для різних статусів Stay
- ⏳ Перевірити обмеження доступу (Wi-Fi тільки для occupied)
- ⏳ Перевірити валідацію токенів (прострочені, відкликані)
- ⏳ Перевірити права доступу (admin/editor можуть генерувати токени)

**Документація:**
- ⏳ Додати OpenAPI схеми для guest endpoints
- ⏳ Оновити Swagger документацію

---

## 📅 Останні зміни (2025-01-26)

### ✅ Додано:
- **Перевірка блокировки редакторів** в `loginAdmin()`:
  - Перевірка `admin.isBlocked` для редактора
  - Перевірка `admin.createdBy?.isBlocked` для редактора
  - Специфічне повідомлення українською мовою для заблокованих користувачів
- **Wi-Fi параметри** в API:
  - Поля `defaultWifiName` та `defaultWifiPassword` в `getUsers()` для superadmin
  - Поля `defaultWifiName` та `defaultWifiPassword` в `getProfile()` для superadmin
  - Підтримка Wi-Fi параметрів в `createAdminBySuperadmin()` та `updateAdminHotelProfile()`
- **Логування** для відлагодження (в dev режимі)

### 🔧 Виправлено:
- Функція `getUsers()` тепер повертає Wi-Fi параметри для суперадміна
- Функція `updateAdminHotelProfile()` повертає Wi-Fi параметри в відповіді
- Покращена обробка помилок у всіх контролерах