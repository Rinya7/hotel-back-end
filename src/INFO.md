# Hotel Backend - Кореневі файли

## Призначення

Кореневі файли проекту - точка входу та налаштування Express додатку.

## Структура

```
src/
├── index.ts          # Точка входу додатку
├── app.ts            # Налаштування Express додатку
└── INFO.md           # Цей файл
```

## Файли

### index.ts

**Призначення:** Точка входу додатку - ініціалізація БД, запуск сервера, graceful shutdown.

**Що робить:**

1. **Ініціалізація:**
   - Завантажує змінні середовища (.env)
   - Імпортує reflect-metadata для TypeORM
   - Ініціалізує підключення до БД

2. **Міграції:**
   - Застосовує міграції (якщо RUN_MIGRATIONS = true)
   - Логує результат

3. **Фонові завдання:**
   - Запускає status scheduler (якщо START_SCHEDULER = true)
   - Автоматичне оновлення статусів кімнат та проживань

4. **HTTP сервер:**
   - Запускає Express сервер на порту з .env
   - Логує URL сервера

5. **Graceful shutdown:**
   - Обробка SIGINT/SIGTERM
   - Зупинка scheduler
   - Закриття HTTP сервера
   - Закриття підключення до БД

6. **Обробка помилок:**
   - Global error handlers для unhandledRejection та uncaughtException

**Налаштування через ENV:**

- `PORT` - порт сервера (за замовчуванням: 3000)
- `RUN_MIGRATIONS` - застосувати міграції (за замовчуванням: true)
- `START_SCHEDULER` - запустити scheduler (за замовчуванням: true)
- `JWT_SECRET` - секретний ключ для JWT (обов'язково)

**Послідовність запуску:**

1. Перевірка JWT_SECRET
2. Ініціалізація БД
3. Застосування міграцій
4. Запуск scheduler
5. Запуск HTTP сервера
6. Налаштування graceful shutdown

### app.ts

**Призначення:** Налаштування Express додатку - middleware, роути, валідація.

**Що робить:**

1. **Безпека:**
   - `helmet()` - захист HTTP заголовків
   - `cors()` - налаштування CORS
   - `rateLimit()` - обмеження кількості запитів

2. **Парсинг:**
   - `express.json()` - парсинг JSON тіла запитів

3. **Документація:**
   - Swagger UI на `/docs`
   - OpenAPI JSON на `/docs.json`

4. **Валідація:**
   - OpenAPI валідатор для всіх запитів/відповідей

5. **Роути:**
   - `/auth` - аутентифікація
   - `/rooms` - кімнати
   - `/stays` - проживання
   - `/audit` - аудит логи
   - `/guest` - гостьовий доступ
   - Room policy routes

**Middleware порядок:**

1. Helmet (безпека)
2. CORS
3. JSON parser
4. Rate limiting
5. Swagger UI
6. OpenAPI Validator
7. Routes

**Важливо:**

- Порядок middleware важливий
- Swagger UI має бути перед валідатором
- Валідатор має бути перед роутами

## Запуск

### Development

```bash
npm run dev
```

### Production

```bash
npm start
```

## Структура проекту

```
src/
├── app.ts              # Express налаштування
├── index.ts            # Точка входу
├── ai/                 # AI-модуль (RAG) - майбутнє
│   ├── rag.ts          # RAG-процесинг (пошук по Stay/Room/Hotel/WiFi/Policies)
│   ├── embeddings.ts   # Ембеддинги (індексація описів номерів, правил, інфи про готель)
│   ├── concierge.ts    # Головна логіка AI-консьєржа
│   └── routes/         # AI роути
│       ├── guest-ai.ts # Публічний endpoint для гостя
│       └── admin-ai.ts # Endpoint для адміна
├── auth/               # Ролі та константи
├── config/             # Конфігурація (БД, OpenAPI, Swagger)
├── constants/          # Константи та значення за замовчуванням
├── controllers/        # Обробники HTTP запитів
├── dto/                # Data Transfer Objects
├── entities/           # TypeORM сутності (моделі БД)
├── jobs/               # Фонові завдання (cron)
├── middlewares/        # Express middleware
├── migrations/         # Міграції БД
├── routes/             # Маршрути API
├── seeds/              # Початкові дані
├── services/           # Бізнес-логіка
├── types/              # TypeScript типи
└── utils/              # Допоміжні функції
```

## Важливі моменти

- **Порядок ініціалізації** - БД → міграції → scheduler → сервер
- **Graceful shutdown** - коректне завершення всіх процесів
- **Error handling** - глобальна обробка помилок
- **Валідація** - автоматична валідація всіх запитів через OpenAPI

## 🔮 Майбутній функціонал

### 🤖 AI-модуль (RAG) для гостей та адміна

**Призначення:**
AI-консьєрж для гостей та AI-асистент для адмінів з використанням RAG (Retrieval-Augmented Generation) технології.

**Структура:**
- `/ai/rag.ts` — RAG-процесинг (пошук по Stay/Room/Hotel/WiFi/Policies)
- `/ai/embeddings.ts` — ембеддинги (індексація описів номерів, правил, інфи про готель)
- `/ai/concierge.ts` — головна логіка AI-консьєржа
- `/ai/routes/guest-ai.ts` — публічний endpoint для гостя
- `/ai/routes/admin-ai.ts` — endpoint для адміна

**Функціональність:**

**Для гостей:**
- Відповіді про заселення, Wi-Fi, правила, інфо про готель
- Погода, запит на прибирання
- Smart-chat: AI знає stayId, roomNumber, статус, дати, Wi-Fi, токен гостя

**Для адмінів:**
- Швидкі відповіді по проживаннях
- Пошук, фільтри, інфа по кімнатах
- Автоматичне формулювання відповідей на основі реальних даних Stay/Room

**RAG доступ до:**
- Stay (дати, guestName, статус)
- Room (статус, політики, Wi-Fi)
- HotelInfo (адреса, контакти, правила, check-in/out)
- FAQ та логи

**API endpoints:**
- `POST /guest-ai/chat` — чат для гостей (authorized by GuestToken)
- `POST /admin-ai/chat` — чат для адміна (JWT)

**Цілі:**
- 5 хвилин інтеграції → робочий AI-консьєрж
- Підтримка мов: uk/en/it/de
- Авто-генерація відповідей на основі реальних даних Stay/Room

### 📅 Календар бронювання номерів

**Призначення:**
Візуальний календар зайнятості номерів для адмінів/редакторів та публічний календар доступності для бронювання.

**Backend API:**
- `GET /rooms/calendar` — календар зайнятості всіх номерів (для адміна/редактора)
- `GET /rooms/calendar/:roomNumber` — календар конкретного номера
- `GET /rooms/availability/calendar` — календар доступності для бронювання (публічний)
- `POST /rooms/search-available` — пошук вільних номерів за критеріями

**Параметри пошуку:**
- `from` / `to` — діапазон дат
- `capacity` — мінімальна місткість номера
- `floor` — поверх (опціонально)
- `status` — фільтр по статусу (опціонально)

**Структура файлів:**
- `/controllers/calendarController.ts` — обробка запитів календаря
- `/services/calendarService.ts` — бізнес-логіка календаря
- `/types/calendar.ts` — типи для календаря

**Функціональність:**
- Розрахунок зайнятості на основі активних Stay
- Відображення конфліктів дат
- Фільтрація по критеріях користувача
- Оптимізовані запити для великих діапазонів дат

---

**Останнє оновлення:** 2025-01-XX

