// src/constants/defaults.ts
// ============================================================================
// КОНСТАНТЫ ЗНАЧЕНИЙ ПО УМОЛЧАНИЮ (BACKEND)
// ============================================================================
//
// ⚠️ ВАЖНО: Этот файл содержит значения по умолчанию для бэкенда.
// Здесь можно легко изменить настройки для всей системы.
//
// ============================================================================

/**
 * Варианты заглушек логотипа отеля
 * Используются, если у отеля не указан logo_url
 *
 * 🔧 ИЗМЕНИТЬ: Добавьте или замените URL на свои варианты логотипов
 * ⚠️ ВАЖНО: Также измените DEFAULT_HOTEL_LOGO_VARIANTS в admin-frontend/src/constants/defaults.ts
 */
export const DEFAULT_HOTEL_LOGO_VARIANTS = [
  "https://img.icons8.com/doodle/48/cottage--v1.png",
  "https://img.icons8.com/?size=100&id=dX32QCBLPsC3&format=png&color=000000",
  "https://img.icons8.com/?size=100&id=qqrPX2IAbe6L&format=png&color=000000",
] as const;

/**
 * Основная заглушка логотипа отеля (используется по умолчанию)
 * Можно изменить на любой из вариантов выше
 */
export const DEFAULT_HOTEL_LOGO_URL = DEFAULT_HOTEL_LOGO_VARIANTS[0];

/**
 * Получить случайный логотип из вариантов
 * Используется при создании нового отеля без указанного logo_url
 */
export function getRandomHotelLogo(): string {
  const randomIndex = Math.floor(
    Math.random() * DEFAULT_HOTEL_LOGO_VARIANTS.length
  );
  return DEFAULT_HOTEL_LOGO_VARIANTS[randomIndex];
}

/**
 * Час заселения по умолчанию
 * Используется в entity Admin и при создании отелей, если не указано другое значение
 *
 * 🔧 ИЗМЕНИТЬ: Измените на нужный час (0-23)
 * Текущее значение: 14 (14:00 / 2 PM)
 *
 * ⚠️ ВАЖНО: Также измените DEFAULT_CHECK_IN_HOUR в admin-frontend/src/constants/defaults.ts
 */
export const DEFAULT_CHECK_IN_HOUR = 14;

/**
 * Час выселения по умолчанию
 * Используется в entity Admin и при создании отелей, если не указано другое значение
 *
 * 🔧 ИЗМЕНИТЬ: Измените на нужный час (0-23)
 * Текущее значение: 10 (10:00 / 10 AM)
 *
 * ⚠️ ВАЖНО: Также измените DEFAULT_CHECK_OUT_HOUR в admin-frontend/src/constants/defaults.ts
 */
export const DEFAULT_CHECK_OUT_HOUR = 10;
