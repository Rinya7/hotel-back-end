🧠 ПІДКЛЮЧЕННЯ ДО БД (DBeaver)
ssh -L 5432:localhost:5432 root@46.224.81.114


У DBeaver:

Host: localhost
Port: 5432
Database: hotel_lotse
User: hotel
Password: 067D446v99A39!


🔥 Тож правильний процес оновлення backend:
    🔵 Локальна розробка
    API працює на: http://localhost:3000
    👉 локально пушиш

🔵 Завантаження змін на сервер
    git add .
    git commit -m "session fix"
    git push

🔵 Деплой на сервері
    cd /opt/hotel/backend
    git pull
    docker compose build
    docker compose up -d
    docker logs -f hotel-backend


Подробнее:
1) Увійти на сервер
ssh root@46.224.81.114

2) Перейти в папку проєкту
cd /opt/hotel/backend

3) Оновити код з GitHub

(обов’язково перед будь-яким перезапуском!)

 
git pull
 

🚀 4) ПЕРЕЗІБРАТИ BACKEND-КОНТЕЙНЕР

(цей крок найважливіший після змін у коді)

docker compose build
docker compose up -d


✔ перебудує контейнер
✔ оновить npm install
✔ згенерує dist
✔ підніме backend з новим кодом
✔ db не рухає

5) Перезапустити тільки backend (швидкий варіант)

Якщо не потрібно rebuild, а тільки restart:

docker compose restart backend

6) Подивитися логі backend у реальному часі
docker logs -f hotel_backend


Вийти → Ctrl + C

7) Увійти всередину контейнера backend
docker exec -it hotel_backend bash


Ти у /app — це твій backend-код.

8) Запустити міграції вручну (якщо потрібно)

(коли зміни у схемі БД)

npm run db:migrate

9) Сидинг супер-адміна
npm run seed:superadmin

10) Якщо контейнер не стартує — перевірити помилки
docker ps -a
docker logs hotel_backend

11) Повне очищення і rebuilding (рідко)
docker compose down
docker compose build --no-cache
docker compose up -d

12) Перевірити, що backend живий
curl http://127.0.0.1:3000


Очікуєш:

Hotel backend is running!

13) Перевірити, що контейнер активний
docker ps | grep backend

14) Перезапуск PostgreSQL (лише якщо треба)
docker compose restart db

 
🧱 НОВА ПРОДУКТИВНА СТРУКТУРА НА СЕРВЕРІ
/opt/hotel-lotse/
   /backend/         ← hotel-backend (API)
   /admin/           ← admin (Vue)
   /guest/           ← guest (PWA)
   /global/ (пізніше) ← публічний сайт
   /service-pages/   ← сервісні сторінки
   /traefik/ або /nginx/  ← для реверс-проксі

✔ Кожен проєкт окремо запускається
cd backend
docker compose up -d

cd admin
docker compose up -d

cd guest
docker compose up -d


✔ Кожен проект має свій Dockerfile

✔ без workspaces
✔ без магії
✔ максимально простий.

✔ Кожен проект самодостатній

свої залежності

свій lock-файл

свій деплой

свій логи

✔ Легко додавати нові проєкти

Хоч 3, хоч 10.

✔ Це ідеально для:

готельного SaaS

глобального сайту

майбутнього RAG-консьєржа

гостьового PWA

адмінки

бекенду

сервісних сторінок
