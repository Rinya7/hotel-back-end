# Бек (Node + TypeORM + PostgreSQL)
- Любая смена контрактов API сопровождается комментариями и миграциями.
- DTO типизируем строго. Никаких `any`.
- Не импортировать ничего из frontend. Только свои entity/DTO/утилиты.
